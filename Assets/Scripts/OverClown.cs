using UnityEngine;

public class OverClown : MonoBehaviour
{
    //after an amount of time, attack cycle...
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
